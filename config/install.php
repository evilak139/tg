<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 安装锁文件路径
    |--------------------------------------------------------------------------
    |
    | 对应07文档：是否已安装用文件锁判断，不查数据库。路径做成可配置是为了测试时能
    | 换成临时文件，测试进程不管是正常结束还是中途崩溃，都不会碰到这个真实路径，
    | 不会把开发机的"安装状态"意外锁死或解锁。
    |
    */
    'lock_path' => storage_path('app/installed.lock'),
];
